"use strict";
(() => {
    // null - undefined
    // stricknullchecks
    const nada = undefined;
    console.log(nada);
    const isActive = true;
    //isActive = undefined // error
    // isActive = null // error
    console.log(isActive);
})();
